spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keystone_dev
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true

jwt:
  secret: ${JWT_SECRET:dev-secret-change-me-please-make-this-long-enough-for-hs256}
  expiration-ms: 86400000