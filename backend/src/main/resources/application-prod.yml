spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    show-sql: false

keystone:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000
  scheduler:
    sla-check-cron: "0 */15 * * * *"